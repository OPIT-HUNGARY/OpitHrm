// Generated by CoffeeScript 1.6.3
(function() {
  var suite;

  casper.test.begin('Login page test', 5, suite = function(test) {
    casper.start("http://notes.local/app_dev.php/secured/login", function() {
      var documentDataFuncs, jQueryPlugins;
      documentDataFuncs = this.evaluate(function() {
        return $(document).data('OpitNotesUserBundle').funcs.userEdit != null;
      });
      test.assert(documentDataFuncs, "User edit function exists");
      jQueryPlugins = this.evaluate(function() {
        return ($.fn.formIsEmpty != null) && ($.fn.checkAll != null);
      });
      test.assert(jQueryPlugins, "jQuery plugins exist");
      test.assertTitle("NOTES", "Login page title is the one expected");
      test.assertExists('form#login', "Login form is found");
      this.fill('form#login', {
        _username: "test0Name"
      }, true);
    });
    casper.then(function() {
      test.assertSelectorHasText("div.error", "Bad credentials");
    });
    casper.run(function() {
      test.done();
    });
  });

}).call(this);
