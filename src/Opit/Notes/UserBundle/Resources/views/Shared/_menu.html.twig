{# must return the controller name #}
{% set module = splitText(app.request.attributes.get('_route'), '_', 1) %}
<nav class="menu">
    <div>
    {% if is_granted('ROLE_ADMIN') %}
    <div class="mainMenu {{ (module == 'admin') ? 'active' : '' }}">
        Admin
        <div class="subMenu">
            <div class="subMenuItem">
                <a href="#" class='dropdown'>User</a>
                <ul>
                    <li>
                        <a href='{{ path('OpitNotesUserBundle_admin_groups_list') }}'>Role management</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
    {% endif %}
    {% if is_granted('ROLE_ADMIN') %}
    <div class="mainMenu {{ (module == 'user') ? 'active' : '' }}" id="userMenuTab">
        Users
        <div class="subMenu">
            <div class="subMenuItem">
                <a href="{{ path('OpitNotesUserBundle_user_list') }}">List</a>
            </div>
        </div>
    </div>
    {% endif %}
    {# TravelRequestBundle #}
    <div class="mainMenu {{ (module == 'travel') ? 'active': '' }}">
        Travel
        <div class="subMenu">
            <div class="subMenuItem">
                <a href="{{ path('OpitNotesTravelBundle_travel_list') }}">List</a>
            </div>
            <div class="subMenuItem">
                <a href="{{ path('OpitNotesTravelBundle_travel_show') }}">Create</a>
            </div>
        </div>
    </div>
</nav>
<nav class="menu"></nav>