// Generated by CoffeeScript 1.7.1
(function() {
  $.extend(true, $(document).data('notes'), {
    funcs: {
      changeTimeSheetStatus: function(statusId, timeSheetStatusId, $spinner) {
        return $.ajax({
          method: 'POST',
          url: Routing.generate('OpitNotesTravelBundle_timesheet_state'),
          data: {
            'statusId': statusId,
            'timeSheetStatusId': timeSheetStatusId
          },
          global: false
        }).done(function(data) {
          return location.reload();
        }).complete(function() {
          return $spinner.remove();
        }).fail(function(data) {
          var $changeState;
          $spinner.remove();
          $changeState = $('.changeState[data-tr="' + timeSheetStatusId + '"]').removeClass('dropdown-disabled').prop('selectedIndex', 0);
          return $('<div id="dialog-tr-error"></div>').html('Status could not be changed due to an error.').dialog({
            title: '<i class="fa fa-exclamation-triangle"></i> An error occurred',
            width: 550,
            buttons: {
              Close: function() {
                $(this).dialog('destroy');
              }
            }
          });
        });
      }
    }
  });

}).call(this);
