// Generated by CoffeeScript 1.7.1
(function() {
  $(document).ready(function() {
    var styles;
    $("#team-leaves-calendar").fullCalendar({
      editable: false,
      selectable: false,
      events: Routing.generate('OpitNotesLeaveBundle_calendar_team_employees')
    });
    styles = '';
    $('.team-employee').each(function() {
      var backgroundColor, elementClass;
      elementClass = $(this).data('class');
      backgroundColor = "rgb(" + (Math.floor(Math.random() * 150)) + ", " + (Math.floor(Math.random() * 150)) + ", " + (Math.floor(Math.random() * 150)) + ")";
      return styles += "." + elementClass + " { background: " + backgroundColor + " !important; }";
    });
    return $("<style>" + styles + "</style>").appendTo('head');
  });

}).call(this);
